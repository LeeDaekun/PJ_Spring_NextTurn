<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nextturn.persistence.MemberDAO">

	<insert id="memInsert">
		INSERT INTO tbl_member(id, pw, name, phone, email, postcode, addr1, addr2) 
		VALUES (#{id}, #{pw}, #{name}, #{phone}, #{email}, #{postcode}, #{addr1}, #{addr2})
	</insert>
			
	<!-- 아이디 중복체크  (아이디 가있으면1 없으면0)-->
	<select id="idOverLap" resultType="int">
		SELECT COUNT(*) FROM tbl_member 
		WHERE id = #{id}
	</select>
	
	
	
	<!-- useyn에는 난수 20개를 넣어라 -->
	<update id="getKey">
		UPDATE tbl_member 
		SET useyn = #{param2} 
		WHERE id = #{param1}
	</update>
	
	
</mapper>
